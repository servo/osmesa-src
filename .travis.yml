language: rust
os:
  - osx
  - linux
install:
  - sudo python -m pip install Mako
addons:
  apt:
    packages:
    - libva-dev
    - clang-3.5
rust:
  - nightly
notifications:
  webhooks: http://build.servo.org:54856/travis

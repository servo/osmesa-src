addons:
  apt:
    packages:
      - libva-dev
      - llvm-3.5-dev
install:
  - pip install --user mako
language: rust
rust:
  - stable
  - beta
  - nightly
cache: cargo
notifications:
  webhooks: http://build.servo.org:54856/travis
